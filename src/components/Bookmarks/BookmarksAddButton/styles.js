import { makeStyles } from "@material-ui/core";
const bookmarksAddButtonStyles = makeStyles({
  box: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    minHeight: 73,
  },
  addButton: {
    height: 50,
    width: 50,
  },
});
export default bookmarksAddButtonStyles;
